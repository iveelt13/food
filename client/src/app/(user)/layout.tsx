import { UserContextProvider } from "@/provider/UserProvider";

const UserPage = () => {
  return;
  <UserContextProvider>
    <div>UserPage</div>;
  </UserContextProvider>;
};

export default UserPage;
